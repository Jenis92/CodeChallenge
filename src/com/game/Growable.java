package com.game;

/**
 * The Growable interface defines a contract for objects that have the ability to grow.
 * Implementing classes should provide the specific details of how the object grows.
 */
public interface Growable {

     /**
      * Increases the size or length of the object.
      * The implementation of this method should specify how the object grows.
      */
     void grow();
}
